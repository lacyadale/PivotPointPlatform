# Server Configuration
PORT=5000
NODE_ENV=development

# Database (when ready)
DATABASE_URL=

# Authentication (when ready)
JWT_SECRET=
JWT_EXPIRES_IN=7d

# External APIs (when ready)
OPENAI_API_KEY=
